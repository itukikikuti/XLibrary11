image: alpine

before_script:
- "apk update"
- "apk add curl"
- "apk add doxygen"

test:
  script:
  - doxygen doxygen/Doxyfile
  except:
  - master
  
pages:
  script:
  - "doxygen doxygen/Doxyfile"
  - "mv doxygen/output/html/ public/"
  - "curl -# -H 'Accept: application/json' -H 'Content-type: application/json' -X POST -d '{\"content\":\"ビルドが完了しました。\"}' 'https://discordapp.com/api/webhooks/490435280973725697/zqhEWJfpxirgOe0A9E4DsU-xRfpc29hxDHsoAia5yFWEr2HwRFP03kKQMUOKdxgOtE1u'"
  - "echo $CI_PROJECT_NAME"
  - "grep -H '' /etc/*version ; grep -H '' /etc/*release"
  artifacts:
    paths:
    - public
  only:
  - master
